# ParkPredict Environment Configuration
# Mapbox Public Token (Optional - ParkPredict automatically falls back to CartoDB/OpenStreetMap if not provided)
VITE_MAPBOX_TOKEN=

# Auth Provider Configuration (Optional - defaults to secure local session management)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
